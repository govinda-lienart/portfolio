---
title: Stage 1 · Frame extraction and MySQL storage
description: Turning raw aquarium video into a structured, frame-level dataset with persistent metadata.
---

import Abstract from '../../../../components/Abstract.astro';
import Figure from '../../../../components/Figure.astro';
import FigureSrc from '../../../../components/FigureSrc.astro';
import PaperHeader from '../../../../components/PaperHeader.astro';
import hevcFaded from '../../../../assets/aquamind/stage-01/hevc-faded.png';
import h264Settings from '../../../../assets/aquamind/stage-01/h264-settings.png';
import sqlVerify from '../../../../assets/aquamind/stage-01/sql-verify.png';

<PaperHeader
  subtitle="Turning raw aquarium video into a structured, frame-level dataset with persistent metadata."
  venue="AquaMind · Stage 1"
  meta="Videos: IMG_*.MOV · OpenCV · MySQL 8.4 (Docker) · 1 frame/sec"
/>

<Abstract>
Stage 1 converts raw aquarium video into a structured, frame-level dataset in
two steps: each video is registered once in a MySQL `videos` table, then
sampled at one frame per second into a `frames` table foreign-keyed back to
it, so later stages can reference a specific frame by number instead of
re-decoding video. The result is a reproducible ingestion layer: consistent
1 FPS sampling, lossless PNG frames on disk, and structured metadata across
two relational tables in a Dockerised MySQL database, forming the foundation
the annotation and training stages build on.
</Abstract>

## Introduction

Annotation needs still, addressable images, not a continuously playing video.
A bounding box in LabelStudio is drawn on one specific frame. That frame must
be findable again by frame number later, when its annotation is stored, and
re-decoding the source `.MOV` on demand cannot guarantee that. Stage 1 builds
that addressable image set: it establishes
the first two of AquaMind's five MySQL tables (`videos` and `frames`) and
fixes the one-frame-per-second sampling contract that Stage 2's annotation
work and Stage 3's YOLO training dataset are built on. That contract is
specific to this stage's own script, not to the `frames` table as a whole: a
later hard-negative-mining step feeds tracker-flagged crossing frames back
into the same table on its own, event-triggered sampling (Stage 2 covers
that). The tracker itself needs full temporal resolution for its own
matching, so [Stage 4](/aquamind/stages/04-tracker/) reads the raw video
directly instead of going through this table at all.

## Aim

Build a database where a video's frames can live: indexed by frame number and
timestamp, so Stage 2's annotation work has something stable to label.

## Methods

<FigureSrc
  src="/aquamind/stages/01-frame-extraction-pipeline.svg"
  alt="Stage 1 video-registration and frame-extraction pipeline. Raw aquarium video, recorded on an iPhone 14 at 60 FPS and switched from HEVC to H.264 after a colour-fidelity bug, is first registered by sync_videos.py, which reads video_metadata_2026_5_30.xlsx and inserts one row per video into the MySQL videos table. extract_frames.py then normalises the reported frame rate with round() and samples one frame per second via frame_count modulo fps. Each selected frame is written twice: as a lossless PNG file on disk, and as a row in the MySQL frames table (frame_number, timestamp, frame_path, video_id foreign key). Both outputs converge into a single frame-indexed dataset that later stages reference by frame number and timestamp instead of re-decoding the source video, feeding forward into Stage 2's LabelStudio annotation step."
  caption="Stage 1 pipeline: sync_videos.py registers each video in MySQL's videos table before extract_frames.py samples it at 1 FPS, writing each frame twice (a PNG on disk and a row in the frames table, foreign-keyed back to videos). The two outputs converge into the frame-indexed dataset every later stage builds on. Cylinders mark the two persistent MySQL tables; boxes mark scripts and file output. Click to zoom."
/>

### Video-encoding constraints

Initial recordings using iPhone 14 (iOS 26) in HEVC (H.265) format introduced
frame-level colour inconsistencies when processed with OpenCV, resulting in
degraded colour fidelity during extraction.

This issue comes from how inter-frame compression works: modern codecs store
occasional full frames and encode only the pixel differences between them to
reduce file size. When OpenCV's software decoder processes an HEVC-encoded file,
colour reconstruction can break down during this decoding process, producing
washed-out frames.

<Figure src={hevcFaded} alt="Faded frame colours due to HEVC codec" caption="Faded colours extracted from an HEVC-encoded MOV file." width={700} />

To ensure deterministic frame decoding, the recording format on iPhone was
switched to H.264 ("Most Compatible" mode). Although this increased storage size,
it significantly improved frame consistency and colour stability during
extraction.

<Figure src={h264Settings} alt="iPhone camera settings switched to Most Compatible mode" caption="Correct colour reproduction after switching to H.264." width={700} />

Recordings were standardised at 60 FPS to increase temporal resolution, enabling
finer capture of fast behavioural events such as feeding strikes.

### Infrastructure setup

A MySQL 8.4 instance runs inside a Docker container, isolated from any local
database installation, with port 3306 exposed so both the Python scripts and
MySQL Workbench can connect to it.

### Database schema

Stage 1 creates two relational tables: `videos`, one row per registered
recording, written by `sync_videos.py`, and `frames`, one row per extracted
frame, written by `extract_frames.py` and foreign-keyed back to its parent
video. Splitting registration from extraction this way means a video's
metadata (species, tank dimensions, filming date) is recorded once, not
repeated on every one of its frame rows, and a `UNIQUE (video_id,
frame_number)` constraint on `frames` makes it impossible to record the same
frame index twice for the same video.

<FigureSrc
  src="/aquamind/stages/01-frame-extraction-schema.svg"
  alt="Entity-relationship diagram of the videos and frames MySQL tables. videos holds one row per registered video: id (primary key), file_path (unique, not null), fps, resolution, activity, plants, fish_count, notes, filmed_at, species, morph, and the three tank-dimension columns. frames holds one row per extracted frame: id (primary key), video_id (foreign key referencing videos.id, not null), frame_path, frame_number, timestamp, and extracted_at, with a unique constraint on (video_id, frame_number). A crow's-foot relationship line connects the two: a single bar near videos marks the one side, a three-pronged crow's foot near frames marks the many side, meaning one video has many frames."
  caption="Entity-relationship diagram for the two tables Stage 1 creates, in standard crow's-foot notation. video_id in frames is a foreign key into videos.id: one video, many frames."
/>

The full DDL for all five of AquaMind's MySQL tables (the three added by
later stages are covered where they're introduced) is in
[aquamind_dump.sql](https://github.com/govinda-lienart/AquaMind/blob/main/aquamind_dump.sql)
on GitHub.

### Video registration

[View source on GitHub](https://github.com/govinda-lienart/AquaMind/blob/main/scripts/sync_videos.py)

`sync_videos.py` reads one row per video from `video_metadata_2026_5_30.xlsx`:
species, morph, tank dimensions, activity, fish count, and filming date, the
experimental metadata that isn't in the video file itself. `fps` and
`resolution` are not taken from the spreadsheet; they're read directly from
the video with OpenCV, so they can't drift out of sync with the actual file.

The insert is an upsert keyed on `file_path` (`INSERT ... ON DUPLICATE KEY
UPDATE`), so re-running the script is safe: an already-registered video gets
its metadata refreshed instead of duplicated, and `register_video()` reports
back which of the two happened.

### Frame-extraction pipeline

[View source on GitHub](https://github.com/govinda-lienart/AquaMind/blob/main/scripts/extract_frames.py)

**Design overview.** The pipeline reads video files sequentially using OpenCV and
extracts frames at a fixed rate of one frame per second. Each extracted frame is
saved both as an image file and as a structured record in a MySQL database.

**Frame-rate normalisation.** The frame rate of each video is retrieved directly
using OpenCV. However, iPhone recordings give a frame rate of `59.94` FPS instead
of a clean `60` FPS due to underlying NTSC video-encoding standards. If used
directly, this small discrepancy causes `frame_count % fps == 0` to select frames
inconsistently. The FPS value is wrapped in `round()` to normalise it to the
nearest integer before being used in the sampling logic.

**Sampling strategy.** Frames are selected using the expression
`frame_count % fps == 0`, which ensures that exactly one frame per second is
extracted. The video is read frame by frame using OpenCV, which returns a boolean
`ret` and the `frame` image data on each call. When `ret` becomes `False`, the
video has reached its end and the loop terminates.

**Frame storage.** Each selected frame is saved as a PNG image to preserve full
image quality without compression loss. This matters because the dataset is
intended for future annotation and machine-learning tasks where pixel-level
accuracy counts. For every frame the system records its index and computes its
timestamp in seconds relative to the start of the video using `frame_count / fps`.

## Results

Ingestion correctness was verified by querying `frames` directly: sequential
frame indexing aligned with the sampling rate, consistent 1-second timestamp
increments, valid file-path references to the stored PNG frames, and correct
separation between different extraction runs in the filesystem.

<Figure src={sqlVerify} alt="Verification SQL table structure and inserts" caption="An early verification query against frames, from before the schema was split into videos and frames (this frames still carries a video_path column directly). The same check, sequential frame_number, one-second timestamp steps, valid frame_path values, applies to the current two-table schema described above." width={700} />

## Discussion

Stage 1 establishes a reproducible ingestion pipeline in two steps: `sync_videos.py`
registers each video once, and `extract_frames.py` samples it at a temporally
consistent 1 FPS. Reliable H.264 decoding, lossless PNG frames on disk, and
metadata split across the `videos` and `frames` MySQL tables together form the
foundational dataset layer for subsequent annotation, labelling and model
training.
